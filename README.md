<p align="center">
  <img src="https://capsule-render.vercel.app/api?type=waving&height=180&color=gradient&text=cub3D%20Project%20%7C%2042%20Barcelona&fontAlign=50&fontAlignY=40&fontSize=40&desc=Raycasting%20Engine%20in%20C&descAlignY=60&descAlign=50" />
</p>



# 🎮 cub3D — Raycasting Maze Game (School 42 Project)

## Описание проекта
**Cub3D** — это графический проект школы 42, целью которого является создание "3D" игры в стиле легендарного **Wolfenstein 3D** (1992).
![Status](https://img.shields.io/badge/status-finished-brightgreen.svg)

### Технологии:

- **Язык:** C ![Language](https://img.shields.io/badge/language-C-blue.svg)
- **Графическая библиотека:** [MLX42](https://github.com/codam-coding-college/MLX42) ![MiniLibX](https://img.shields.io/badge/MiniLibX-active-green.svg)
- **Компилятор:** gcc
- **Стандарт кода:** Norminette (школа 42) ![Norminette](https://img.shields.io/badge/style-Norminette-green.svg)
- **Платформа:** Linux

### Задачи проекта:

-  Прочитать и валидировать файл конфигурации и карты `.cub`
-  Реализовать **ray casting** для псевдо-3D рендеринга
-  Реализовать управление игроком (WASD + повороты стрелками/мышью)
-  Применить текстуры на стены в зависимости от их ориентации
-  Установить разные цвета для пола и потолка

---

## 📋 Содержание

- [Структура проекта](#структура-проекта)
- [Что такое Ray Casting](#что-такое-ray-casting)
- [Фаза 1: Парсинг и валидация](#фаза-1-парсинг-и-валидация)
  - [Формат файла .cub](#формат-файла-cub)
  - [Чтение файла](#чтение-файла)
  - [Парсинг конфигурации](#парсинг-конфигурации)
  - [Парсинг карты](#парсинг-карты)
  - [Валидация карты](#валидация-карты)
  - [BFS для проверки замкнутости](#bfs-для-проверки-замкнутости)
- [Фаза 2: Ray Casting и DDA](#фаза-2-ray-casting-и-dda)
- [Фаза 3: Рендеринг](#фаза-3-рендеринг)
- [Установка и запуск](#установка-и-запуск)
- [Ресурсы](#ресурсы)

## Структура проекта

Проект условно разделён на **3 основные фазы**:

### 1️⃣ Чтение и валидация файла `.cub`
- Парсинг конфигурации (текстуры, цвета)
- Парсинг карты лабиринта
- Валидация: корректность символов, наличие игрока, замкнутость карты

### 2️⃣ Реализация алгоритмов Ray Casting и DDA
- Инициализация лучей для каждого столбца экрана
- DDA (Digital Differential Analyzer) для обхода сетки
- Расчёт расстояния до стен без искажения "рыбий глаз"

### 3️⃣ Рендеринг изображения
- Отрисовка пола и потолка
- Применение текстур на стены
- Соответствие столбца экрана и столбца текстуры
- Управление игроком и обновление кадров

---

## Что такое Ray Casting?

**Ray Casting** — техника рендеринга псевдо-3D графики (2.5D), которая создаёт иллюзию трёхмерного пространства на основе двумерной карты.

### Основная идея:
**Для каждого вертикального столбца экрана:**
1. Выпускаем луч из позиции игрока
2. Луч летит по карте, пока не врежется в стену
3. Измеряем расстояние до стены
4. Рисуем вертикальную полоску: чем ближе стена — тем выше полоска

**Результат для разрешения 640x480:** 640 вертикальных полосок разной высоты, которые создают иллюзию 3D-лабиринта.

Эта техника позволяла запускать игры на процессорах начала 90-х годов — никакой настоящей 3D-графики, только умная математика!

---
---

## 📦 Структура `.cub` файла

Файл .cub содержит всю необходимую информацию для запуска игры:

- Пути к текстурам четырёх стен (`NO`, `SO`, `WE`, `EA`)
- Цвета пола и потолка (`F`, `C`) в формате `R,G,B`
- Карту лабиринта из символов `0`, `1`, `N`, `S`, `E`, `W`

Пример файла карты:

```text
NO ./textures/north_wall.png
SO ./textures/south_wall.png
WE ./textures/west_wall.png
EA ./textures/east_wall.png

F 220,100,0
C 225,30,0

        1111111111111111111111111
        1000000000110000000000001
        1011000001110000000000001
        1001000000000000000000001
111111111011000001110000000000001
100000000011000001110111111111111
11110111111111011100000010001
11110111111111011101010010001
11000000110101011100000010001
10000000000000001100000010001
10000000000000001101010010001
11000001110101011111011110N0111
11110111 1110101 101111010001
11111111 1111111 111111111111
```
1️⃣ Инициализация структур и чтение файла (file.c)
-  инициализация игрового состояния init_game(), MLX и изображения init_engine(). 
- В init_game_data() обнуляются структуры, выставляются дефолтные значения.
- Открываем файл .cub
- Читаем его построчно с помощью get_next_line (из libft)
- Сохраняем все строки в динамический массив

- Проверки:
- ✅ Количество аргументов при вызове
- ✅ Расширение файла — .cub
- ✅ Файл существует и доступен для чтения
- ✅ Файл не пустой

2️⃣ Парсинг конфигурации (parse_config.c, parse_texture.c, parse_color.c)

Находим начало карты map_start. Всё что выше - конфиг, всё что ниже - карта. Парсим конфиг:
- Разбираем строки с идентификаторами NO, SO, WE, EA, F, C
- Извлекаем пути к текстурам и значения RGB
- Строка: `NO ./textures/north_wall.png`
- Извлекаем:
- - идентификатор: `NO`
- - путь: `./textures/north_wall.png`

Проверки для текстур:

- ✅ Идентификатор корректный (NO/SO/WE/EA)
- ✅ Путь указан и не пустой
- ✅ Файл текстуры существует
- ✅ Формат файла — .png
- ✅ Каждая текстура указана ровно один раз

Парсинг цветов:

- Строка: `F 220,100,0`
- Извлекаем:
- - идентификатор: `F` (floor)
- - RGB: R=220, G=100, B=0

Проверки для цветов:

- ✅ Формат: R,G,B (без пробелов вокруг запятых)
- ✅ Каждое значение — число от 0 до 255
- ✅ Ровно 3 компонента (R, G, B)
- ✅ F и C указаны ровно один раз
**Конвертация RGB в hex:**
- R=220, G=100, B=0
- hex = (R << 16) | (G << 8) | B
- hex = 0xDC6400

3️⃣ Парсинг карты (parse_map.c, map.c)

- Считываем все строки карты в двумерный массив char map->grid
- Определяем ширину и высоту карты
- Допустимые символы:

- 0 — пустое пространство (проходимо)
- 1 — стена (непроходимо)
- N, S, E, W — стартовая позиция и направление взгляда игрока
- (пробел) — пустота за пределами карты

4️⃣ Проверка закрытости карты (bfs.c)
BFS vs Flood fill
Безопасность: Нет риска переполнения стека на больших картах
Контроль: Ты явно управляешь очередью, а не полагаешься на стек вызовов
Читаемость: Итеративный код проще отлаживать

Карта в cub3D должна быть полностью окружена стенами (1), чтобы игрок не мог "выйти" за её пределы. Если в карте есть дыра (пробел   на границе доступной области), это приведёт к неопределённому поведению при рендеринге.
Как работает BFS в проекте
Общий алгоритм:
Инициализация:

Создаём матрицу visited размером [height][width] (все значения = 0)
Создаём круговую очередь размером height × width
Добавляем стартовую позицию игрока в очередь
Помечаем стартовую клетку как посещённую (visited[y][x] = 1)
Обход в ширину:

Пока очередь не пуста:
Извлекаем клетку из очереди
Проверяем 4 соседних клетки (вверх, вниз, влево, вправо)
Для каждого соседа:
Если это 0 (пустая клетка) → помечаем как посещённую, добавляем в очередь
Если это 1 (стена) → игнорируем
Если это   (пробел) → ОШИБКА! Карта не замкнута
Результат:

Если все доступные клетки проверены без встречи с пробелами → карта валидна ✅
Если встретили пробел → карта невалидна ❌
typedef struct s_point
{
    int x;  // Координата X
    int y;  // Координата Y
} t_point;
Круговая очередь (Circular Queue):
C
typedef struct s_queue
{
    t_point *data;         // Массив точек
    int     first;         // Индекс первого элемента
    int     last;          // Индекс последнего элемента
    int     current_size;  // Текущее количество элементов
    int     capacity;      // Максимальная вместимость
} t_queue;
Почему круговая?

Эффективное использование памяти: после dequeue место освобождается и переиспользуется
O(1) для операций enqueue и dequeue
Избегаем постоянных сдвигов элементов массива

Общий алгоритм:
Инициализация:

Создаём матрицу visited размером [height][width] (все значения = 0)
Создаём круговую очередь размером height × width
Добавляем стартовую позицию игрока в очередь
Помечаем стартовую клетку как посещённую (visited[y][x] = 1)
Обход в ширину:

Пока очередь не пуста:
Извлекаем клетку из очереди
Проверяем 4 соседних клетки (вверх, вниз, влево, вправо)
Для каждого соседа:
Если это 0 (доступная клетка) → помечаем как посещённую, добавляем в очередь
Если это 1 (стена) → игнорируем (граница игровой зоны)
Если это   (пробел) → ОШИБКА! Доступная клетка граничит с пустотой — карта не замкнута
Результат:

Если все доступные клетки проверены без встречи с пробелами → карта замкнута ✅
Если хотя бы одна доступная клетка граничит с пробелом → карта незамкнута ❌

Данные для BFS:
C
typedef struct s_bfs_data
{
    int     **visited;  // Матрица посещённых клеток (0 или 1)
    t_queue *queue;     // Очередь для обхода
} t_bfs_data;
Основные функции
init_bfs — инициализация
C
t_bfs_data *init_bfs(t_map *map);
Создаёт матрицу visited (все значения = 0)
Создаёт очередь размером height × width
Добавляет стартовую позицию игрока
Помечает стартовую клетку как посещённую
process_bfs — основной обход
C
int process_bfs(t_map *map, t_bfs_data *bfs);
Извлекает клетку из очереди
Проверяет 4 соседних клетки
Возвращает 1 (валидна) или 0 (невалидна)
heck_cell — проверка клетки
C
int check_cell(t_map *map, t_point pos, t_queue *queue, int **visited);
Проверяет символ в клетке:
0 → добавить в очередь
1 → игнорировать (стена)
  → вернуть 0 (дыра!)
can_visit — можно ли посетить
C
int can_visit(t_point pos, t_map *map, int **visited);
Проверяет границы карты
Проверяет, не посещалась ли уже клетка
Преимущества нашей реализации
✅ Безопасность: Нет риска переполнения стека
✅ Эффективность: O(width × height) — каждая клетка проверяется ровно один раз
✅ Память: O(width × height) для visited + O(width × height) для очереди
✅ Полнота: Гарантированно обходит все доступные клетки
✅ Детекция дыр: Моментально находит пробелы на границах
✅ Читаемость: Итеративный код легко отлаживать
Что такое "замкнутая" карта?
Замкнутая карта — это карта, где все доступные для игрока клетки (символы 0, N, S, E, W) окружены стенами (1). Игрок не должен иметь возможности "выйти" за пределы игровой зоны.

Ключевой критерий незамкнутости:
❌ Карта незамкнута, если любая доступная клетка (0) граничит с пробелом ( ).

Другими словами:

✅ Клетка 0 может соседствовать с 0 (другая доступная клетка)
✅ Клетка 0 может соседствовать с 1 (стена — граница игровой зоны)
❌ Клетка 0 НЕ МОЖЕТ соседствовать с   (пробел — пустота за картой)
Почему пробелы валидны, но опасны?
Пробел ( ) — это легальный символ в файле .cub. Он используется для:

Выравнивания карты (отступы слева)
Обозначения "ничего" за пределами игровой зоны
Пример валидного использования пробелов:

Code
        11111111  ← Пробелы слева для отступа (OK)
        10000001
        100N0001
        10000001
        11111111
Но если пробел граничит с доступной клеткой — это дыра:

Code
11111111
1000000   ← Пробел справа граничит с `0` — ДЫРА!
100N0001
10000001
11111111




























Структуры данных для хранения
C
typedef struct s_map
{
    char    **grid;        // Двумерный массив карты
    int     width;         // Ширина (макс. длина строки)
    int     height;        // Высота (количество строк)
} t_map;

typedef struct s_textures
{
    mlx_texture_t   *north;    // Текстура северной стены
    mlx_texture_t   *south;    // Текстура южной стены
    mlx_texture_t   *west;     // Текстура западной стены
    mlx_texture_t   *east;     // Текстура восточной стены
} t_textures;

typedef struct s_player
{
    double  pos_x;         // Позиция X (в координатах карты)
    double  pos_y;         // Позиция Y
    double  dir_x;         // Направление взгляда X
    double  dir_y;         // Направление взгляда Y
    double  plane_x;       // Камера-плоскость X (FOV)
    double  plane_y;       // Камера-плоскость Y
} t_player;

typedef struct s_game
{
    t_map       map;
    t_textures  textures;
    t_player    player;
    uint32_t    floor_color;   // Цвет пола (hex)
    uint32_t    ceiling_color; // Цвет потолка (hex)
    mlx_t       *mlx;
    mlx_image_t *image;
} t_game;


Ключевые файлы проекта (фаза 1)
Файл	Назначение
file.c	Чтение файла построчно
parse_config.c	Основной парсер конфигурации
parse_texture.c	Парсинг текстур (NO/SO/WE/EA)
parse_color.c	Парсинг цветов (F/C)
parse_map.c	Парсинг карты лабиринта
validation.c	Валидация символов и игрока
bfs.c	BFS для проверки замкнутости
queue.c	Реализация очереди для BFS
map_utils.c	Утилиты для работы с картой
parse_utils.c	Вспомогательные функции парсинга
